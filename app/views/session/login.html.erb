<%= form_with url: :login_validation_path do |f|%>
  <div class="form-group">
    <%= f.label :email %>
    <%= f.email_field :email, class: 'form-control', required: true%>
  </div>
  <div class="form-group">
    <%= f.label :password %>
    <%= f.password_field :password, class: 'form-control', required: true %>
  </div>
  <%= f.submit 'Iniciar sesión', class: 'btn btn-primary', id: 'submitLogin'%>
<% end %>
